
const MemoryStatusByClientHint = ({ clientHintDeviceMemory }) => (
  <h4>{`[Client Hint Device Memory from Server Side Rendering: ${clientHintDeviceMemory} GByte]`}</h4>
);

export default MemoryStatusByClientHint;
